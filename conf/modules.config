process {
    withName: CAT_FASTQ {
        publishDir = [
            path: { "${params.outdir}/results/merged" },
            mode: 'copy',
            pattern: "*.fastq.gz",
	    saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }
}
