// SLURM configuration for HPC cluster
process {
    executor = 'slurm'
    queue = 'cpu'
    clusterOptions = '--account=singers'
}

executor {
    $slurm {
        queueSize = 100
        submitRateLimit = '10 sec'
    }
}
